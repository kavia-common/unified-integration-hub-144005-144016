# PUBLIC_INTERFACE
# NEXT_PUBLIC_BACKEND_URL is used by the frontend to call the backend APIs.
# Set this to your backend URL (including protocol and port).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
